# PEKAT Bridge Overview (Deferred Integration)

## Intent
Prepare a clean transfer package so this standalone renderer can be embedded later into `PEKAT_inspection_tool_byPJ`.

## Current status
- No integration code is implemented here.
- This repository only provides implementation-ready documentation and context assets.

## Reusable building blocks
- `src/liss_render.py` (core visualization logic)
- `src/audio_capture.py` (source abstractions and ring buffer)
- `src/main.py` argument surface and runtime orchestration

## Out of scope now
- PEKAT runtime hooks
- PEKAT UI embedding
- PEKAT deployment scripts
